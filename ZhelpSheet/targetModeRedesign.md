1) I need a main actor. When selected it ends
2) I need an action selected and a turn commenced
3) Checks target type, then allows additional targets based on what target its on
3.1) Single is just one target, aoe is any number (enter, and button from targetMode means end), Self is just itself
4) Sends all characters involved down alongside all bonuses with ids, so bonuses know where to go 
5) Backend proccess all the bonuses and make this a promise as it has to load quite a lot. 