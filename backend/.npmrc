#multer-storage-cloudinary@4 declares a peer dep on cloudinary@^1, but works fine with the
#maintained cloudinary@2 SDK at runtime (see config/cloudinary.js). npm v7+ rejects the peer
#mismatch and fails `npm ci` on clean installs (Railway), so fall back to npm v6 peer behaviour.
legacy-peer-deps=true
